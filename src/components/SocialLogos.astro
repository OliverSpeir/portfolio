---
const { class: className } = Astro.props;
---
<nav class={className}>
    <a href="https://www.linkedin.com/in/oliverspeir/" target="_blank" class="tooltip" data-tooltip="Click to view my LinkedIn profile"><img src="/LinkedIn_Logo.svg"/></a>
    <a href="https://github.com/OliverSpeir" target="_blank" class="tooltip" data-tooltip="Click to view my GitHub profile"><img src="/GitHub_Logo.svg"/></a>
    <a href="mailto:oliverspeir9@gmail.com" target="_blank" class="tooltip" data-tooltip="Click to send me an email"><img src="/mail_icon.svg"/></a>
    <a href="https://storage.googleapis.com/oliver-speir-cv-resume/OliverSpeirCV.pdf" target="_blank" class="tooltip" data-tooltip="Click to see resume"><img src="/resume.svg"/></a>
</nav>

<style>
    .projects {
      display: flex;
      justify-content: center;
      gap: 1.5vw;
      margin-top: 1vh;
    }
    .projects img {
      width: auto;
      height: 3.5vh;
    }
    .projects .tooltip {
        position: relative;
        display: inline-block;
    }
    .projects .tooltip::after {
        content: attr(data-tooltip);
        background: var(--card-color);
        color: var(--text-color);
        padding: 0px 10px 5px 10px;
        position: absolute;
        z-index: 1;
        top: 100%;
        left: 50%;
        transform: translateX(-50%);
        white-space: nowrap;
        opacity: 0;
        transition: opacity .5s ease-in-out;
        pointer-events: none;
        border-radius:20px;
    }
    .bottom {
      display: flex;
      justify-content: center;
      gap: 1.5vw;
      margin-top: 1vh;
    }
    .bottom img {
      width: auto;
      height: 5vh;
    }
    .bottom .tooltip {
        position: relative;
        display: inline-block;
    }
    .bottom .tooltip::after {
        content: attr(data-tooltip);
        background: var(--card-color);
        color: var(--text-color);
        padding: 0px 10px 5px 10px;
        position: absolute;
        z-index: 1;
        top: 100%;
        left: 50%;
        transform: translateX(-50%);
        white-space: nowrap;
        opacity: 0;
        transition: opacity .5s ease-in-out;
        pointer-events: none;
        border-radius:20px;
    }
    .projects .tooltip:hover::after {
        opacity: 1;
    }
    .home {
      display: flex;
      justify-content: center;
      gap: 1.5vw;
      margin-top: 1vh;
    }
    .home img {
        margin-top:2vh;
        width: auto;
        height: 3vh;
    }
    .home .tooltip {
        position: relative;
        display: inline-block;
    }
    .home .tooltip::after {
        content: attr(data-tooltip);
        background: var(--card-color);
        color: var(--text-color);
        padding: 0px 10px 5px 10px;
        position: absolute;
        z-index: 1;
        top: 100%;
        left: 50%;
        transform: translateX(-50%);
        white-space: nowrap;
        opacity: 0;
        transition: opacity .5s ease-in-out;
        pointer-events: none;
        border-radius:20px;
    }

    .home .tooltip:hover::after {
        opacity: 1;
    }
    @media (max-width:874px){
        .home {
            display:none;
        }
    }
    @media (min-width:875px){
        .bottom {
            display:none;
        }
    }
</style>