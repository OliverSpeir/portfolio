---

// it's  IIFE https://developer.mozilla.org/en-US/docs/Glossary/IIFE

// IIFE are useful for data-astro-rerun scripts because the window is preserved when using Astro's <ViewTransitions/>

/* unminified code 

(() => {
  const formatter = new Intl.DateTimeFormat("en-US", {
    timeZone: "America/Denver",
    hour12: false,
    hour: "2-digit",
    minute: "2-digit",
  });

  // Format the date and remove leading zero from hours
  let timeInTimeZone = formatter.format(new Date()).replace(/^0/, "");

  // Determine AM/PM indicator
  const amPmIndicator = parseInt(timeInTimeZone.split(":")[0], 10) < 12 ? " AM" : "";

  // Update the display
  const timeDisplay = document.getElementById("time");
  if (timeDisplay) timeDisplay.innerText = timeInTimeZone + amPmIndicator;
})();

*/
---
<script is:inline data-astro-rerun>
(()=>{let e=new Intl.DateTimeFormat("en-US",{timeZone:"America/Denver",hour12:!1,hour:"2-digit",minute:"2-digit"}).format(new Date).replace(/^0/,"");const t=parseInt(e.split(":")[0],10)<12?" AM":"",n=document.getElementById("time");n&&(n.innerText=e+t)})();
</script>
